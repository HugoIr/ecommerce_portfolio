import 'package:flutter/material.dart';

class OfficialStore extends StatelessWidget {
  const OfficialStore({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
